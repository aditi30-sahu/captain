#include<stdio.h>
int sum(int);
int main()
{
   int n;
   scanf("%d",&n);
   printf("the sum of the digits of the number is %d",sum(n));
}
int sum(int num)
{
  int ans=0;
  while(num!=0)
  {ans+=num%10;
  num=num/10;}
  return ans;
}
