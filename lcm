#include<stdio.h>
int lcm(int,int );
int main()
{
  int x,y;
  scanf("%d %d",&x,&y);
  printf("The lcm of %dand %d is %d",x,y,lcm(x,y));
}
int lcm(int a,int b)
{
  int i,gcd;
  for(i=1;i<=a &&i<=b;i++)
 { if(a%i==0 && b%i==0)
  {gcd=i;}}
  return a*b/gcd;
  
}
